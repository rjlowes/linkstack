import React from 'react';

const Header = () => (
    <header className="header">
        <nav>
            <span>Folders</span>
        </nav>
        <a>LinkStack</a>
        <nav>
            <span>nav</span>
        </nav>
    </header>
);

export default Header;
